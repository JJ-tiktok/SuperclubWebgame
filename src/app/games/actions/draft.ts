/**
 * Draft server actions.
 *
 * Hosts the single action that drives the snake-draft pick flow:
 *   - makeDraftPickAction
 *
 * Implementation currently lives in `src/app/games/actions.ts`.
 */

export { makeDraftPickAction } from "@/app/games/actions";

